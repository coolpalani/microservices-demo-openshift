apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: front-end-virtualservice
spec:
  hosts:
    - 'front-end-sock-shop.apps.cluster-f19b.f19b.sandbox1401.opentlc.com'
  gateways:
    - sockshop-wildcard-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        port:
          number: 80
        host: front-end.user1-sockshop.svc.cluster.local
